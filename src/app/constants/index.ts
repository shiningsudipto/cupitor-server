export enum Roles {
  User = 'user',
  Admin = 'admin',
}
export enum OrderStatus {
  Pending = 'pending',
  InProgress = 'inprogress',
  Delivering = 'delivering',
  Delivered = 'delivered',
  Canceled = 'canceled',
}
export enum OrderItemStatus {
  Pending = 'pending',
  Packed = 'packed',
}
